#include "GeneralUser.h"

GeneralUser::GeneralUser(string name, int resiNum, string ID, string PW){
    userName = name;
    ResidentRegistrationNumber = resiNum;
    userID = ID;
    userPassword = PW;
}